{
  "name": "paperforge",
  "version": "1.0.0",
  "description": "AI-powered LaTeX writing tool for researchers",
  "private": true,
  "packageManager": "pnpm@10.12.2",
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "build": "pnpm --recursive run build",
    "test": "pnpm --recursive run test",
    "lint": "pnpm --recursive run lint",
    "clean": "pnpm --recursive run clean"
  },
  "keywords": ["latex", "ai", "research", "academic", "writing"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
